define([
    'require',
    'jquery',
    'jquery-parallax',
    'jasmine-jquery'
], function(require){
    var $ = require('jquery'),
        test = function(){
            describe('jquery-parallax', function(){

                beforeEach(function(){

                });

                afterEach(function(){

                });

                xit('No spec', function(){
                    pending('Specs yet to be determined');
                })
            })
        };

    return {
        run : test
    };
});