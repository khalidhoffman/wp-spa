define([
    'require',
    'ngApp'
], function (require) {
    var ngApp = require('ngApp');
    return ngApp.controller('mainController', ['$scope', '$rootScope', function ($scope, $rootScope) {

    }])
});