wp_spa_require([
    'require',
    'live',
    'ng-router',
    'directives',
    'controllers'
], function (require) {
    angular.bootstrap(document, ['dp-spa']);

    //$body.append($testBtn);

});
